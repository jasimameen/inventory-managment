import 'package:flutter/material.dart';

class ScreenPrevBills extends StatelessWidget {
  const ScreenPrevBills({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('ScreenPrevBills'),),
    );
  }
}