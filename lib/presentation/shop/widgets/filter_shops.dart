import 'package:flutter/material.dart';

class FilterShops extends StatelessWidget {
  const FilterShops({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}