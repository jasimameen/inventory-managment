import 'package:flutter/material.dart';

class ScreenRoute extends StatelessWidget {
  const ScreenRoute({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('ScreenRoute'),),
    );
  }
}